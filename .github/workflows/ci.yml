name: CI

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main"]

env:
  PYTHON_DEFAULT: "3.12"
  RUST_TOOLCHAIN: "stable"

jobs:
  python-tests:
    name: Python ${{ matrix.python-version }} tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      # Both test roots. `tests/` was previously excluded, which is why an
      # orphaned module there could sit broken without failing CI.
      - name: Run tests
        run: |
          python -m pytest jasusi_cli/tests/ tests/ \
            -v --tb=short \
            --timeout=60
        env:
          JASUSI_TEST_MODE: "1"

      - name: ruff
        run: python -m ruff check jasusi_cli app.py tests

      - name: mypy strict
        run: |
          python -m mypy jasusi_cli/ \
            --strict \
            --ignore-missing-imports

  rust-check:
    name: Rust cargo check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust ${{ env.RUST_TOOLCHAIN }}
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          components: clippy

      - uses: arduino/setup-protoc@v3
        with:
          version: "25.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: cargo check
        run: cargo check --workspace --manifest-path rust/Cargo.toml

      - name: clippy
        run: cargo clippy --workspace --all-targets --manifest-path rust/Cargo.toml -- -D warnings

      - name: cargo test
        run: cargo test --workspace --manifest-path rust/Cargo.toml

  smoke-tests:
    name: Smoke tests (no API key)
    runs-on: ubuntu-latest
    needs: python-tests
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_DEFAULT }}
          cache: "pip"

      - name: Install package
        run: pip install -e ".[dev]"

      - name: jasusi --version
        run: jasusi --version

      - name: jasusi status
        run: jasusi status

      - name: pip install smoke test
        run: python -c "import jasusi_cli; print(jasusi_cli.__version__)"
